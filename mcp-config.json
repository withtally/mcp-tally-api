{
  "mcpServers": {
    "tally-api": {
      "command": "bun",
      "args": ["run", "start"],
      "env": {
        "TALLY_API_KEY": "your_api_key_here",
        "TRANSPORT_MODE": "stdio"
      }
    }
  }
}
